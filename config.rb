UserSettings = {
  # If your remote user name is different to local user name.
  # user_name: ""

  # Path to xcode dmg file.
  xcode_dmg_path: "Xcode_7.2.1.dmg"
  # Alternatively, you can `rsync --particial` the dmg file to
  # `$HOME/Downloads`.
  # Make sure there is only one file matching `Xcode*.dmg`
  # in `$HOME/Downloads`.
}
